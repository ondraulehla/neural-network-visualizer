frontend:
# Deploy to Cloud Run
gcloud run deploy neural-network-frontend \
  --source . \
  --platform managed \
  --region us-central1 \
  --allow-unauthenticated

backend:
# Deploy to Cloud Run
gcloud run deploy neural-network-backend \
  --source . \
  --platform managed \
  --region us-central1 \
  --allow-unauthenticated

  gcloud run deploy neural-network-backend \   --source . \
  --platform managed \
  --region us-central1 \
  --allow-unauthenticated \
  --set-env-vars "GOOGLE_CLOUD_PROJECT=neural-network-config"